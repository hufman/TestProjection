<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.hufman.testprojection">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".MainService"
            android:label="@string/app_name"
            android:exported="false">
            <intent-filter>
                <action android:name="me.hufman.testprojection.MainService.start" />
                <action android:name="me.hufman.testprojection.MainService.stop" />
            </intent-filter>
        </service>

        <service android:name="me.hufman.carprojection.adapters.ICarService">
            <intent-filter>
                <action android:name="me.hufman.testprojection.FakeCar" />
            </intent-filter>
        </service>
        <service android:name="me.hufman.carprojection.adapters.ICarProjectionCallbackService">
            <intent-filter>
                <action android:name="me.hufman.testprojection.FakeCarProjectionCallback" />
            </intent-filter>
        </service>
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Allows this app to host Android Auto applications" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
    </application>

</manifest>